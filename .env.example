# Turso (production) — get these from https://turso.tech/app
DATABASE_URL="libsql://your-db-name.turso.io"
DATABASE_AUTH_TOKEN="your-turso-auth-token"

# Local SQLite (development) — no auth token needed
# DATABASE_URL="file:local.db"
# DATABASE_AUTH_TOKEN=""
